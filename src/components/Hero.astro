---
import CodeWindow from "../ui/CodeWindow.astro";
import FolatingDot from "../ui/decoratives/FolatingDot.astro";

const [skill1, skill2] = ["Angular", "Spring Boot"];
---

<section class="hero">
   <!-- Decoration -->
   <FolatingDot top="10%" right="-150px" animationDuration="6s" />
   <FolatingDot
      size="200px"
      bgColor="var(--secondary-gradient)"
      bottom="20%"
      left="-100px"
      animationDuration="8s"
      animationDirection="reverse"
   />
   <FolatingDot
      size="150px"
      bgColor="var(--accent-gradient)"
      top="60%"
      right="20%"
      animationDuration="7s"
   />
   <!-- <div class="floating-dots"> -->
   <div class="dot"></div>
   <div class="dot"></div>
   <div class="dot"></div>
   <div class="dot"></div>
   <div class="dot"></div>
   <!-- </div>

   <!-- Main content -->
   <div class="hero-container">
      <div class="hero-content">
         <div class="hero-text">
            <h1 class="hero-title">
               <span class="name">Álvaro González</span>
               <span class="role">Full-Stack Developer</span>
            </h1>
            <p class="hero-description">
               Creo experiencias digitales excepcionales combinando diseño
               elegante con código limpio. Especializado en <strong
                  >{skill1}</strong
               >, <strong>{skill2}</strong> y tecnologías modernas.
            </p>
            <div class="hero-buttons">
               <button class="btn btn-primary">
                  <span>Ver Proyectos</span>
                  <svg width="20" height="20" viewBox="0 0 24 24" fill="none">
                     <path
                        d="M7 17L17 7M17 7H7M17 7V17"
                        stroke="currentColor"
                        stroke-width="2"
                        stroke-linecap="round"
                        stroke-linejoin="round"></path>
                  </svg>
               </button>
               <button class="btn btn-secondary">
                  <span>Contactar</span>
               </button>
            </div>
            <div class="tech-stack">
               <span class="tech-label">Tech Stack:</span>
               <div class="tech-icons">
                  <div class="tech-item">React</div>
                  <div class="tech-item">Node.js</div>
                  <div class="tech-item">TypeScript</div>
                  <div class="tech-item">MongoDB</div>
               </div>
            </div>
         </div>

         <div class="hero-visual">
            <CodeWindow
               name="Álvaro González"
               skill1={skill1}
               skill2={skill2}
               passion="Building amazing apps"
            />
         </div>
      </div>
   </div>

   <!-- Indicador de scroll -->
   <div class="scroll-indicator">
      <div class="scroll-text">Scroll</div>
      <div class="scroll-arrow"></div>
   </div>
</section>

<style>
   .hero {
      min-height: 100vh;
      position: relative;
      display: flex;
      align-items: center;
      background: radial-gradient(
            ellipse at top,
            rgba(102, 126, 234, 0.1) 0%,
            transparent 50%
         ),
         radial-gradient(
            ellipse at bottom right,
            rgba(245, 87, 108, 0.1) 0%,
            transparent 50%
         );
   }

   .dot {
      position: absolute;
      width: 4px;
      height: 4px;
      background: rgba(255, 255, 255, 0.3);
      border-radius: 50%;
      animation: twinkle 3s ease-in-out infinite;
   }

   .dot:nth-child(1) {
      top: 20%;
      left: 20%;
      animation-delay: 0s;
   }

   .dot:nth-child(2) {
      top: 40%;
      left: 80%;
      animation-delay: 1s;
   }

   .dot:nth-child(3) {
      top: 70%;
      left: 30%;
      animation-delay: 2s;
   }

   .dot:nth-child(4) {
      top: 30%;
      left: 60%;
      animation-delay: 1.5s;
   }

   .dot:nth-child(5) {
      top: 80%;
      left: 70%;
      animation-delay: 0.5s;
   }

   /* Container principal */
   .hero-container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 2rem;
      position: relative;
      z-index: 2;
   }

   .hero-content {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 4rem;
      align-items: center;
   }

   /* Contenido de texto */
   .hero-text {
      animation: slideInLeft 1s ease-out;
   }

   .wave {
      font-size: 1.5rem;
      animation: wave 2s ease-in-out infinite;
   }

   .hero-title {
      font-size: clamp(2.5rem, 5vw, 4rem);
      font-weight: 700;
      line-height: 1.1;
      margin-bottom: 1.5rem;
   }

   .name {
      display: block;
      background: var(--primary-gradient);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
   }

   .role {
      display: block;
      color: var(--text-secondary);
      font-size: 0.7em;
      font-weight: 400;
      margin-top: 0.5rem;
   }

   .hero-description {
      font-size: 1.2rem;
      line-height: 1.6;
      color: var(--text-secondary);
      margin-bottom: 2rem;
      max-width: 500px;
   }

   /* Botones */
   .hero-buttons {
      display: flex;
      gap: 1rem;
      margin-bottom: 3rem;
   }

   .btn {
      padding: 1rem 2rem;
      border: none;
      border-radius: 50px;
      font-size: 1rem;
      font-weight: 500;
      cursor: pointer;
      transition: all 0.3s ease;
      display: flex;
      align-items: center;
      gap: 0.5rem;
      text-decoration: none;
      position: relative;
      overflow: hidden;
   }

   .btn-primary {
      background: var(--primary-gradient);
      color: white;
      box-shadow: 0 10px 30px rgba(102, 126, 234, 0.3);
   }

   .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 15px 40px rgba(102, 126, 234, 0.4);
   }

   .btn-secondary {
      background: transparent;
      color: var(--text-primary);
      border: 2px solid var(--border-color);
      backdrop-filter: blur(10px);
   }

   .btn-secondary:hover {
      background: var(--card-bg);
      border-color: rgba(255, 255, 255, 0.3);
      transform: translateY(-2px);
   }

   /* Tech Stack */
   .tech-stack {
      display: flex;
      align-items: center;
      gap: 1rem;
      flex-wrap: wrap;
   }

   .tech-label {
      color: var(--text-secondary);
      font-size: 0.9rem;
   }

   .tech-icons {
      display: flex;
      gap: 0.5rem;
      flex-wrap: wrap;
   }

   .tech-item {
      padding: 0.5rem 1rem;
      background: var(--card-bg);
      border: 1px solid var(--border-color);
      border-radius: 25px;
      font-size: 0.8rem;
      backdrop-filter: blur(10px);
      transition: all 0.3s ease;
   }

   .tech-item:hover {
      background: rgba(255, 255, 255, 0.1);
      transform: translateY(-2px);
   }

   /* Visual del código */
   .hero-visual {
      animation: slideInRight 1s ease-out;
   }

   /* Indicador de scroll */
   .scroll-indicator {
      position: absolute;
      bottom: 2rem;
      left: 50%;
      transform: translateX(-50%);
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 0.5rem;
      color: var(--text-secondary);
      font-size: 0.8rem;
      z-index: 2;
   }

   .scroll-arrow {
      width: 20px;
      height: 20px;
      border: 2px solid currentColor;
      border-top: none;
      border-left: none;
      transform: rotate(45deg);
      animation: bounce 2s infinite;
   }

   /* Animaciones */
   @keyframes twinkle {
      0%,
      100% {
         opacity: 0.3;
      }

      50% {
         opacity: 1;
      }
   }

   @keyframes wave {
      0%,
      100% {
         transform: rotate(0deg);
      }

      25% {
         transform: rotate(20deg);
      }

      75% {
         transform: rotate(-20deg);
      }
   }

   @keyframes slideInLeft {
      from {
         opacity: 0;
         transform: translateX(-50px);
      }

      to {
         opacity: 1;
         transform: translateX(0);
      }
   }

   @keyframes slideInRight {
      from {
         opacity: 0;
         transform: translateX(50px);
      }

      to {
         opacity: 1;
         transform: translateX(0);
      }
   }

   @keyframes bounce {
      0%,
      20%,
      50%,
      80%,
      100% {
         transform: translateY(0) rotate(45deg);
      }

      40% {
         transform: translateY(-10px) rotate(45deg);
      }

      60% {
         transform: translateY(-5px) rotate(45deg);
      }
   }

   /* Responsive */
   @media (width < 872px) {
      .hero-content {
         grid-template-columns: 1fr;
         gap: 2rem;
         text-align: center;
      }

      .hero-visual {
         order: -1;
      }

      .hero-buttons {
         justify-content: center;
      }

      .tech-stack {
         justify-content: center;
      }

      .shape-1,
      .shape-2,
      .shape-3 {
         display: none;
      }
   }

   @media (width < 480px) {
      .hero-container {
         padding: 0 1rem;
      }

      .hero-buttons {
         flex-direction: column;
         align-items: center;
      }

      .btn {
         width: 100%;
         max-width: 250px;
         justify-content: center;
      }
   }
</style>
